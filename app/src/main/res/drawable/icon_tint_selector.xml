<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
        <!-- Active (selected) icon color -->
        <item android:color="@color/bottomActive" android:state_checked="true" />

        <!-- Passive (unselected) icon color -->
        <item android:color="@color/bottomUnActive" android:state_checked="false"/>

</selector>